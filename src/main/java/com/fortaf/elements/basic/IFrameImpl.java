package com.fortaf.elements.basic;

import org.openqa.selenium.WebElement;

import com.fortaf.elements.base.ElementImpl;

/**
 * Wrapper class that wraps IFrame functionality.
 */
public class IFrameImpl extends ElementImpl implements IFrame {

	public IFrameImpl(WebElement element) {
		super(element);
		
	}

}
